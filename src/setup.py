#! /usr/bin/env python

from distutils.core import setup

SCRIPT = "waitend"

setup(name=SCRIPT,
        version="0.1.0",
        scripts=[SCRIPT],
        url="https://github.com/sblondon/%s" % SCRIPT,
      )

